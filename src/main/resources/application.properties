spring.application.name=Express

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/express_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=I0lXBc:&FX8bYMRxPkQoLt318Yv>uhak@E7<0PQrycs
jwt.expiration=86400000
jwt.refresh.expiration=604800000

# Server Configuration
server.port=8080

# Rate Limiting Configuration
# Auth endpoints: 5 requests per minute (prevents brute force attacks)
# API endpoints: 100 requests per minute (normal usage)
rate.limit.auth.capacity=5
rate.limit.api.capacity=100
